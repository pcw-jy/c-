/*

C语言动态内存申请注意事项
 
一、常用函数
 
1.  malloc(size_t n) ：分配未初始化堆内存
2.  calloc(int num,size_t size) ：分配并初始化为0
3.  realloc(指针,新大小) ：扩容/缩容内存
4.  free(指针) ：释放堆内存
 
二、核心注意事项
 
1. 必须判断申请是否成功
malloc失败返回 NULL ，不判断直接使用会崩溃
 

  
int *p = malloc(4);
if(p == NULL) return -1;
 
 
2. 用完必须free，防止内存泄漏
只申请不释放，程序运行越久占用内存越大。
3. 禁止重复free
同一内存多次free直接程序崩溃。
4. free后置空野指针
free后指针仍指向原地址，变成野指针
 
  
free(p); p = NULL;
 
 
5. 不能释放栈空间
free只能释放堆内存，不能free局部变量、数组。
6. realloc使用要点
 
- 旧指针不要直接接收返回值，失败会丢原地址
- 扩容失败返回NULL，原内存不变
 
c
  
int *tmp = realloc(p,100);
if(tmp)p=tmp;
 
 
7. 越界读写
动态数组超范围读写，破坏内存，随机闪退。
8. 不要返回局部动态指针
函数内malloc可以返回，不能返回栈地址。
9. 对齐与字节大小
分配字节数算准，int占4字节，char占1字节。
10. 嵌套内存逐层释放
结构体嵌套动态内存，由内到外依次free。
 
三、易错禁忌
 
- 数组名不能free
- 空指针free无害，不会报错
- 动态内存生命周期全局大于局部
- 不要修改malloc返回的原指针地址
 


*/