/*

哈希函数：除数求余法   H(Key) =  Key % p(数组的最大容量)


一、核心概念

哈希查找：不逐个比较遍历，通过一个公式（哈希函数），直接算出数据应该存放在数组的哪个位置，一步定位找到数据，时间复杂度接近 O(1)。

二、原理三步

1. 哈希函数
把关键字（比如学号、姓名、数字）通过公式换算，得到一个数组下标。
例： H(key) = key % 数组长度 
关键字 23，数组长度10 → 下标3，直接存在数组第3个位置。
2. 哈希冲突
两个不同关键字，算出同一个下标，就是冲突。
例：13、23 对10取余都得3，都要放下标3，就冲突了。
3. 解决冲突
常用两种：

- 开放定址法：冲突了就往后找下一个空位存放
- 链地址法（拉链法）：同一个下标挂一条链表，冲突元素都挂在这条链上

三、查找过程

1. 用哈希函数算关键字的下标
2. 直接去该下标位置比对
3. 匹配到就找到；有冲突就按冲突规则往后/沿链表继续找

四、特点

- 优点：极快，理想情况一次定位
- 缺点：有哈希冲突，需要额外处理；占用空间比顺序表稍大



*/